- name: Variable
  hosts: WEB
  vars:
    URL: google.com
  tasks:
    - name: Print Variable
      ansible.builtin.debug:
        msg: URL = {{ URL }}
#ANSIBLE ACCESS varible with two flower brackets
